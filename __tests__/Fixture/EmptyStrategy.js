import MoveStrategy from '../../src/Models/MoveStrategy/MoveStrategy.js';

export default class EmptyStrategy extends MoveStrategy {
    // generateNumber() {} 구현하지 않음
    // isMovable() {} 구현하지 않음
}
