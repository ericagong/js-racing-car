## 피드백 검토 및 반영 사항

### step1 피드백 기반 반영 사항

1. 상수 처리

    [V] Model 내부로 상수 위치 변경

    [V] 상수가 클래스와 결합해야하면 `static` 키워드 사용

    [V] 클래스 내부 상태 은닉 필요하면 `private` 접근 제어자 사용 : #

2. 요구사항 변경 가능지점 체크하여 확장성 고려해 코드 작성

    [V] 자동차 전진 조건이 변경되는 지점 자유

    [V] 자동차 전진 조건이 변경되는 시점 자유

    [V] 자동차 전진 조건 변경에 따른 테스트 코드 변경

3. 보다 객체 지향적으로 설계

    [V] 팩토리 패턴 도입

### 공통 피드백 기반 반영 사항

1.  가독성

-   코딩 컨벤션 변경

    [V] 클래스 내 변수/메소드 구현 순서 통일: 상수 -> 클래스 변수 -> 인스턴스 변수 -> 생성자 -> 메서드

    [V] 개행 방식 통일

    [V] import문 순서 규칙에 따라 통일

    [V] eslint, prettier 환경 설정 도입

-   의미가 드러나도록 코드 작성하기

    [V] 최대한 주석 없애기

    [V] 의미가 드러날 수 있도록 변수/함수명 지정

    [V] 코드만 봐도 충분히 의미를 파악할 수 있게 단순 명료하게 작성

-   함수/메소드는 하나의 역할만 담당하도록 처리

    [V] 입력값 검증과 예외 생성 함수 분리

-   함수/메소드를 최대한 간단하게 작성

    [V] 함수의 인자 수는 3개 미만으로 유지. 그 이상으로 필요하다면 함수/메소드를 분리하거나 객체 형태로 인자 처리

    [V] 함수 depth 2 이상 지양하기. 그 이상으로 필요하다면 함수/메소드를 분리

    [V] early return 사용하기. 예외 케이스는 함수/메소드 앞부분 위치

    [V] boolean값만 리턴하는 함수는 조건문 없이 처리

2.  재사용성

-   JS 빌트인 객체 내장메서드 최대한 활용

    [V] for, while 반복문 대신 Array 객체 내장 메서드 사용

-   명령형보다는 선언형 코드 작성

3.  테스트

    [V] 테스트를 위한 코드는 프로덕션 코드에서 분리

    [V] 단위 테스트 실패 이유는 하나!

    [V] 경계조건 테스트에 parameterized test 방식 사용

## step1 2차 피드백 관련 반영 사항

1. 불필요한 지점 점검하기

    [V] 외부에서 쓰이지 않는 변수, 함수가 public 필드로 선언되어있지
    않은지 점검

    [V] 클래스 내 불필요한 getter, setter 점검

    [V] 굳이 함수화하지 않기 -> 가독성을 낮추는 불필요한 함수화 지양

2. 예외 케이스 추가하기
   espc. Input 값이 의도치 않은 type인 경우 방어 코드 추가

    [V] new Car() 인자가 숫자, {}, [], " ", 함수 케이스
    예외처리하기 -> 테스트 코드

3. 테스트 코드 명확하게 작성하기

    [V] 테스트 코드의 유효한 데이터, 유효하지 않은 데이터 이유로 테스트

    [V] 테스트코드를 이해하기 위한 불필요한 시간 들지 않게 처리하기

    [V] 너무 많은 환경 변수 사용 지양

    [V] 불필요한 변수 저장 하지 않기

4. 객체의 역할과 책임 구분하기

    [V] Car.tryMoveWith() -> Car 객체가 randomNumber를 알필요가 있는가?

    [V] Game.isMovable -> Game이 규픽을 소유하고 있을 필요가
    있는가? 외부에서 받아오기

    [V] Game 객체를 분리하기 -> Cars 객체 추가

5. 함수를 예측 가능하게 만들기

    [V] 랜덤 값 생성 등 예측 불가능한 부분 분리해서 외부 인자로 주입받기
    -> Game에서 random 관리하지 않으려면?

    [V] 예측 불가능한 부분을 외부로 분리해야 테스트 코드 쉽게 작성 가능

### step2 1차 피드백 기반 반영 사항

1. 목적에 맞는 구현 방식 선택

    [V] IIFE -> 일반 함수 변경

    [V] 프로덕션 layer vs 테스트 layer 분리

    [V] Runtime 에러 객체 - Custom 에러 객체 상속 구조 도입

    [V] 존재하는 함수가 있다면, 수레바퀴 재발명 지양

    \* readline Promise 기반 API

2. 테스트 코드 가독성 향상

    [V] 상수 사용 지양

    [V] 굳이 저장할 필요 없다면 변수 사용 지양

    [V] 결과값도 하드코딩 값으로 변경

    [V] testcase 인식 가능하도록 name 변경

    [V] jest.fn, jest.spyOn 지양 -> 역할 분리?

3. 객체의 역할과 책임 분리

    [V] 테스트가 필요한 핵심적인 private 코드가 있다면 역할 분리 고민 -> generateNumber 분리? -> NO

    [V] 객체는 메시지로 소통

### step2 2차 피드백 관련 반영 사항

1. 객체 단일 책임 원칙 준수

-   객체가 4개 이상의 상태를 관리하면, 상태를 동일 `관심사`로 묶어 관리하기
-   혹은 객체 분리의 시점이 아닌지 고민

    [V] state나 callbacks 등의 namespace로 묶기

-   객체 내 메소드 수가 4개 이상이 아니도록 관리
-   이상이라면, 객체 분리의 순간이 아닌지 고민하기(하나 이상의 역할을 하고 있을 가능성이 높음)

    [V] Car 객체에서 유효성 검사 책임 Name 객체로 분리

    [V] Cars 객체에서 round play 하는 함수 외부 분리

    [V] Cars 객체에서 자동차 배열 정보 가져오는 함수 외부 분리

    -   이름 중복 체크 담당
    -   매 Round 플레이: cars랑 게임 전진 조건 받아 플레이 후 결과 반환

    [V] getter 도입해 메소드 수 줄이기

-   역할 위임 잘하기
    [V] 2 level 이상 들어가서 일처리 하지 않고, 명령하기

2. 가독성

2.1 네이밍 컨벤션

-   파일명

    [V] export default 파일명 일치

-   함수명

    [V] 객체 내부 함수 네이밍 시, 모델명 포함 X

    [V] 단, 외부에서 불러다 사용할 때 모델명 붙여주기

-   상수 네이밍

    [V] 상수값이 아니라 도메인 내 상수 역할을 이름으로 사용

2.2 테스트 코드

-   유사한 test code는 describe로 그룹화하거나 병합
-   test 내 들어가는 input과 결과를 한 눈에 보여지도
-   test description을 추상적이지 않고 명확하게 작성: 처음 테스트 코드 읽는 사람이 맥락 모르고도 바로 알게끔!

3. 모듈화

-   자주 사용하는 함수는 utils로 분리

4. 패턴 사용

-   필요하지 않은 단계에서, 가독성을 헤치면서까지 패턴 사용 지양

    [V] MoveStrategy application level 주입

    [V] 숫자에 종속되지 않게 default Condition 변경
